= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
  = f.error_notification
  
  .card-panels
    .row
      .col-md-8.offset-md-2
        %fieldset
          %legend Edit Profile
          %div
            - if current_user.demo_user?
              .profile-form-group
                %label.control-label Email
                %p.form-control-plaintext= current_user.email
              .profile-form-group
                %label.control-label Password
                %p.form-control-plaintext.warning= 'demouser'
                %span.form-text.text-muted= t('parts.demo_user.change_password_hint')
            - else
              .profile-form-group
                = f.input :email, required: true, autofocus: true
              .profile-form-group
                = f.input :password, autocomplete: "off", hint: t("devise.registrations.leave_it_blank_if_dont_want_to_change"), required: false
              .profile-form-group
                = f.input :password_confirmation, required: false
              .profile-form-group
                = f.input :current_password, hint: t("devise.registrations.need_current_password_to_confirm"), required: true
              
              .form-group
                .col-lg-10.offset-lg-2
                  = f.button :submit, t('common.update'), class: "btn btn-primary"

